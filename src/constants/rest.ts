import {restUrl} from '../.env';

export const REST_API = `${restUrl}/api`;
export const REST_PMC = `${restUrl}/pmc`;
export const REST_OAUTH = `${restUrl}/oauth`;
export const REST_OAUTH_LOGOUT = `${restUrl}/oauth/logout`;
